{
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/test/test_clang++",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
